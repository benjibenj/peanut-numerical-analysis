// Animations types, speeds and easing

const fastSpeed = "0.3s";
const defaultSpeed = "0.5s";
const slowSpeed = "1s";

const Animation = {
  fastSpeed,
  defaultSpeed,
  slowSpeed
};

export default Animation;
